# core Packages

if [ "${CBF['os_id']}" = 'alpine' ]; then
    apk add --no-cache curl findutils libxml2 mysql-client nginx openssh-client shadow sudo supervisor thttpd ttf-dejavu unzip util-linux zlib
fi