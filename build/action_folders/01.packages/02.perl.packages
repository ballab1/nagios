# nagios perl Packages

if [ "${CBF['os_id']}" = 'alpine' ]; then
    declare PKGS='openssl-dev perl perl-cgi perl-cgi-session perl-plack perl-dbi perl-dbd-mysql perl-gd perl-rrd'
    package.checkIfInstalled "$PKGS"
    apk add --no-cache $PKGS
fi
