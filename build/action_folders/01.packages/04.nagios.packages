# nagios packages

if [ "${CBF['os_id']}" = 'alpine' ]; then
    apk add --no-cache freetype gd jpeg libpng mrtg mysql nagios-plugins-all
    apk add --no-cache rrdtool rrdtool-cgi rrdtool-utils rsync
fi